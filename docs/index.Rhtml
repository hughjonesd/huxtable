<html>

<head>
<title>Huxtable</title>


<style type="text/css">
p {
  margin-top: 24pt;
  margin-bottom: 12pt;

}

body {
  background-color: #e1e1dd;
  font-family: "Palatino Linotype", "Book Antiqua", Palatino, serif;
}
main {
  background-color: white;
  border: 1px solid black;
  max-width: 800px;
  margin-top: 40px;
  margin-bottom: 40px;
  margin-left: auto;
  margin-right: auto;
  padding: 5% 5% 5% 5%;
}

.source {
  border-radius: 10px;
}
</style>
</head>
<body>

<main>

<!--
<img src="logo.png" style="width: 200px; margin-left: auto;">
-->

<!--begin.rcode logo, echo = FALSE, results = 'asis'

library(huxtable)
hux_logo()

end.rcode-->

<p>&nbsp;</p>

<!-- Generated as a huxtable table, ignoring all rules of modern web design -->

<!--begin.rcode links, echo = FALSE, results = 'asis'

ht <- hux('<a href = "introduction-to-huxtable.html">Introduction</a>', '<a href = "design-principles.html">Design</a>', '<a href = "https://github.com/hughjonesd/huxtable">Github</a>', '<a href = "https://github.com/hughjonesd/huxtable/issues">Bugs</a>')
escape_contents(ht) <- FALSE
right_border(ht)[,1:3] <- 1
width(ht) <- .8
align(ht) <- 'center'
ht
end.rcode-->



<p>Huxtable is a package for creating HTML and LaTeX tables. It provides similar
functionality to xtable, but more, with a simpler interface. Here's a quick example.
</p>

<!--begin.rcode echo = TRUE, results = 'asis'


ht <- hux(
        Employee = c('John Smith', 'Jane Doe', 'David Hugh-Jones'),
        Salary = c(50000, 50000, 40000),
        add_colnames = TRUE
      )

bold(ht)[1,]           <- TRUE
bottom_border(ht)[1,]  <- TRUE
align(ht)[,2]          <- 'right'
right_padding(ht)      <- 10
left_padding(ht)       <- 10
width(ht)              <- 0.35

ht
end.rcode-->


<p>To learn more, check out the vignette in <a href="introduction-to-huxtable.html">HTML</a> or <a href="introduction-to-huxtable.pdf">PDF</a> format, or the original <a href="introduction-to-huxtable.Rmd">R Markdown</a>. Or,
read the <a href="design-principles.html">design principles</a> behind huxtable, including a comparison
with other R packages to create tables.</p>

<h3>Installation</h3>

<p>From CRAN:</p>

<!--begin.rcode CRAN, echo = TRUE, eval = FALSE
install.packages('huxtable')
end.rcode-->

<p>Development version from github:</p>

<!--begin.rcode install, echo = TRUE, eval = FALSE

library(devtools)
install_github('hughjonesd/huxtable')
end.rcode-->

<h3>The logo</h3>
<p>The logo above was also generated by a huxtable, using <code>hux_logo()</code>.</p>

</main>

</body>
</html>

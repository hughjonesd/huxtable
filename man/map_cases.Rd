% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping-functions.R
\name{map_cases}
\alias{map_cases}
\title{Map cell contents to properties using \code{case_when}}
\usage{
map_cases(..., skip_na = TRUE)
}
\arguments{
\item{...}{A list of two-sided formulas interpreted by \code{case_when}.}

\item{skip_na}{Where \code{case_when} returns \code{NA}, leave the value unchanged. Otherwise, \code{NA} will
normally reset the property value to the default.}
}
\value{
A function for use in \code{set_***_by} functions.
}
\description{
This function uses \code{\link[dplyr:case_when]{dplyr::case_when()}} to set cell properties.
}
\details{
Within the formulas, the variable \code{.} will refer to the content of \code{ht[rows, cols]} (converted
by \code{as.matrix}).

\code{case_when} returns \code{NA} when no formula LHS is matched. By default this leaves the old property
value unchanged. To avoid this, set a default in the last formula: \code{TRUE ~ default}.
}
\examples{
if (requireNamespace('dplyr')) {
  ht <- hux(rnorm(5), rnorm(5), letters[1:5])

  set_background_color_by(ht, map_cases(
      . == "a" ~ "red",
      . \%in\% letters ~ "green",
      . < 0 ~ "pink"
  ))
}
}
\seealso{
\link{mapping-functions}

Other mapping functions: \code{\link{map_function}},
  \code{\link{map_matches}}, \code{\link{map_quantiles}},
  \code{\link{map_ranges}}, \code{\link{map_values}}
}
\concept{mapping functions}

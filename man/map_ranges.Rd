% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_by.R
\name{map_ranges}
\alias{map_ranges}
\title{Map numeric ranges to cell properties}
\usage{
map_ranges(breaks, values, right = FALSE, extend = TRUE)
}
\arguments{
\item{breaks}{A vector of numbers in increasing order.}

\item{values}{A vector of property values. \code{length(values)} should be one greater than
\code{length(breaks)} if \code{extend = TRUE}, or one less if \code{extend = FALSE}.}

\item{right}{Intervals are closed on the right. I.e. if values are exactly equal to a \code{break},
put them in the lower group.}

\item{extend}{Extend \code{breaks} to \code{c(-Inf, breaks, Inf)}, i.e. include numbers outside the range.
\code{TRUE} by default.}
}
\value{
A function for use in \code{set_***_by} functions.
}
\description{
\code{map_ranges} sets property values for cells falling within different numeric ranges.
}
\details{
Non-numeric cells are unchanged.
}
\examples{
ht <- huxtable(c(1, 3, 5))
ht <- set_background_color_by(ht, map_ranges(c(2, 4), c("red", "yellow", "blue")))
ht
set_background_color_by(ht, map_ranges(c(2, 4), "pink", extend = FALSE))

set_background_color_by(ht, map_ranges(c(1, 5), c("red", "yellow", "green"), right = TRUE))
set_background_color_by(ht, map_ranges(c(1, 5), c("red", "yellow", "green"), right = FALSE))
}
\seealso{
\link{mapping-functions}

Other mapping functions: \code{\link{map_cases}},
  \code{\link{map_function}}, \code{\link{map_matches}},
  \code{\link{map_quantiles}}, \code{\link{map_values}}
}
\concept{mapping functions}

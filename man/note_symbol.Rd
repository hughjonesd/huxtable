% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/properties-table.R
\name{note_symbol}
\alias{note_symbol}
\alias{note_symbol<-}
\alias{set_note_symbol}
\title{Set the symbols used for cell note references}
\usage{
note_symbol(ht)

note_symbol(ht) <- value

set_note_symbol(ht, value)
}
\arguments{
\item{ht}{A huxtable.}

\item{value}{One of \code{"numeric"}, \code{"roman"} or \code{"alphabetic"}, or a
non-empty string whose individual characters form a custom symbol
sequence. Set to \code{NA} to reset to the default, which is \code{"numeric"}.}
}
\value{
\code{note_symbol()} returns a character string. The replacement function
and \code{set_note_symbol()} return the modified huxtable.
}
\description{
\code{note_symbol} is a table property controlling marks for \code{\link[=cell_note]{cell_note()}}. The
marks themselves are assigned when a huxtable is rendered rather than stored
in its cells, so subsetting does not modify cell-note metadata.
}
\details{
Numeric symbols are \code{1}, \code{2}, ...; Roman symbols are \code{i}, \code{ii}, ...; and
alphabetic symbols are \code{a}, \code{b}, ..., \code{z}, \code{aa}, .... Custom strings are
treated like a custom alphabet. For example, \code{"+*"} produces \code{+}, \code{*},
\verb{++}, \verb{+*}, \verb{*+}, ....

Custom Unicode symbols require support from the output format's font and
rendering engine.
}
\examples{
ht <- set_cell_note(jams, 1, 1, "Price estimated")
ht <- set_note_symbol(ht, "+*")
note_symbol(ht)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/properties-table.R
\name{table_notes}
\alias{table_notes}
\alias{table_notes<-}
\alias{set_table_notes}
\alias{add_table_note}
\title{Set notes beneath a table}
\usage{
table_notes(ht)

table_notes(ht) <- value

set_table_notes(ht, value)

add_table_note(ht, value)
}
\arguments{
\item{ht}{A huxtable.}

\item{value}{A character vector, or \code{NA}/\code{NULL} to remove all notes.}
}
\value{
\code{table_notes()} returns a character vector. The replacement function,
\code{set_table_notes()} and \code{add_table_note()} return the modified huxtable.
}
\description{
Table notes are stored separately from the table's cells. They do not change
the number of rows in the huxtable. Each element of \code{value} is printed as a
separate note beneath the table.
}
\details{
Use \code{NA} or \code{NULL} to remove all table notes.

Table notes are plain text. Huxtable escapes them as required for each output
format.

HTML, LaTeX, Typst and Word output use their native table-footer or table-note
structures. RTF, Markdown and screen output print notes immediately after the
table. Excel writes each note to a merged row below the table. Breakable LaTeX
tables use the \code{threeparttablex} package.

Use \code{\link[=cell_note]{cell_note()}} to add notes referenced from individual cells. Referenced
cell notes are printed after unreferenced table notes.

\code{\link[=add_footnote]{add_footnote()}} is retained for compatibility only. It adds an ordinary,
full-width row to the table and is soft-deprecated in favour of
\code{add_table_note()}.
}
\examples{
ht <- set_table_notes(jams, "Note: Prices exclude delivery.")
ht <- add_table_note(ht, "Source: The jam growers' association.")
table_notes(ht)
table_notes(ht) <- NULL

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_by.R
\name{ranges}
\alias{ranges}
\title{Set properties by numeric intervals}
\usage{
ranges(breaks, values, right = FALSE, extend = TRUE)
}
\arguments{
\item{breaks}{A vector of numbers in increasing order.}

\item{values}{A vector of property values. \code{length(values)} should be one greater than
\code{length(breaks)} if \code{extend = TRUE}, or one less if \code{extend = FALSE}.}

\item{right}{Intervals are closed on the right. I.e. if values are exactly equal to a \code{break},
put them in the lower group.}

\item{extend}{Extend \code{breaks} to \code{c(-Inf, breaks, Inf)}, i.e. include numbers outside the range.
\code{TRUE} by default.}
}
\value{
A function for use in \code{set_***_by} functions.
}
\description{
Set properties by numeric intervals
}
\examples{
ht <- huxtable(c(1, 3, 5))
ht <- set_background_color_by(ht, ranges(c(2, 4), c("red", "yellow", "blue")))
ht
set_background_color_by(ht, ranges(c(2, 4), "pink", extend = FALSE))

set_background_color_by(ht, ranges(c(1, 5), c("red", "yellow", "green"), right = TRUE))
set_background_color_by(ht, ranges(c(1, 5), c("red", "yellow", "green"), right = FALSE))
}
\seealso{
set-by

Other classifier: \code{\link{values}}
}
\concept{classifier}

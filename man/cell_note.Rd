% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/properties-cell.R
\name{cell_note}
\alias{cell_note}
\alias{cell_note<-}
\alias{set_cell_note}
\alias{map_cell_note}
\title{Set notes referenced from cells}
\usage{
cell_note(ht)

cell_note(ht) <- value

set_cell_note(ht, row, col, value)

map_cell_note(ht, row, col, fn)
}
\arguments{
\item{ht}{A huxtable.}

\item{value}{A character vector or matrix. Use \code{NA} or \code{""} for no note.}

\item{row}{A row specifier. See \link{rowspecs} for details.}

\item{col}{An optional column specifier.}

\item{fn}{A mapping function. See \link{mapping-functions} for details.}
}
\value{
\code{cell_note()} returns a character matrix. The replacement function,
\code{set_cell_note()} and \code{map_cell_note()} return the modified huxtable.
}
\description{
\code{cell_note} is a cell property containing plain-text notes. When a huxtable
is rendered, cells with notes receive reference marks and the distinct notes
are printed beneath any unreferenced \code{\link[=table_notes]{table_notes()}}. Identical note text in
multiple cells receives the same mark.
}
\details{
Reference marks are assigned when the huxtable is rendered, in row-major
order over visible cells. Subsetting a huxtable therefore does not modify its
notes. In merged ranges, only a note on the top-left visible cell is used.

HTML, LaTeX, Typst and RTF use superscript marks. Word, Excel, Markdown and
screen output use bracketed marks.

Use \code{\link[=note_symbol]{note_symbol()}} to choose numeric, Roman, alphabetic or custom marks.
}
\examples{
ht <- hux(a = 1:2, b = 3:4)
ht <- set_cell_note(ht, 1:2, 1, "Measured value")
cell_note(ht)

}

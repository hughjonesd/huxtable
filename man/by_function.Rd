% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_by.R
\name{by_function}
\alias{by_function}
\title{Set cell properties using a function or scale}
\usage{
by_function(inner_fn)
}
\arguments{
\item{inner_fn}{A one-argument function which maps cell values to property values.}
}
\value{
A function for use in \code{set_***_by} functions.
}
\description{
This creates a simple wrapper around a function for use in \code{set_xxx_by}.
Useful functions include scales and palettes from the \code{scales} package.
}
\examples{
ht <- huxtable(runif(5), runif(5), runif(5), runif(5))

set_text_color_by(ht, by_function(grey))

if (requireNamespace('scales')) {
  set_background_color_by(ht, by_function(
        scales::col_numeric(c('red', 'orange', 'yellow'), domain = NULL)
  ))
}

if (requireNamespace('scales')) {
  set_text_color_by(ht, by_function( scales::seq_gradient_pal() ))
}
}
\seealso{
\link{set-by}

Other `by` functions: \code{\link{by_matching}},
  \code{\link{by_quantile}}, \code{\link{by_range}},
  \code{\link{by_value}}
}
\concept{`by` functions}
